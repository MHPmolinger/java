package com.neusoft;

public class LearnContainer {
/*
package com.dongruan.learnContainer;

import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.Set;

public class LearnContainer {
/**
 *1、什么是容器：用来盛装值得都可以视为容器  之前学习到的 有哪些  也属于容器？ 数组    变量
 *2、今天要学习的容器类型：ArrayList   Hashset  Hashmap
 *3、泛型：就是强制规定 当前  集合类型 存储内容值类型  为何类型   好处： 可以 规范当前 容器 只存放一种类型数据，
 *       避免  一个容器存放多种类型  不方便 取值或查找
 *       泛型的写法     ArrayList<定义类型>  变量名=new  ArrayList<定义类型> （）；
 *4、ArrayList：首先ArrayList是一个类  赋值使用  add方法  依次向尾部添加元素的形式 ，默认索引位置 从0开始
 *  对比 ArrayList 与数组之间的特点以及区别：
 *  1.数组定长  而 ArrayList不定长
 *  2.数组类型在定义的时候就确定下来  ArrayList 可以存放  所有类型的值
 *  3.取值 二者相同 都是根据索引 往外取值 不同的是   ArrayList取出来的值 是  object类型的 所以  有时候需要对取出来的值 进行强制类型转换（向下转型）
 *5、Hashset：也是一种容器  
 *  HashSet容器 不同于其他的容器种类   它在一定程度上 可以视作是无序的容器 没有  索引的概念  存进去的值 会根据它底层的hash算法进行最为有效率的存放
 *  ArrayLis与Hashset进行比对：
 * 1.ArrayList与Hashset都支持泛型
 * 2.ArrayList允许存放重复内容  Hashset不允许存放重复内容
 * 3.ArrayList有序存放（跟存放顺序一致） Hashset无序存放
 * 4.ArrayList可以根据索引往外取值  Hashset 只能借助迭代器往外取值
 *  
 *6、HashMap是一个比较特殊的   容器  是基于 键值对 形式进行存放 key-value      
 *   key是不允许重复的  即便重复 后面的value也会覆盖之前的value
 *   value是否允许重复呢？value允许重复
 *   如何才能通过循环遍历的形式将所有的value值都取出来？？
 *   
		HashMap  map=new HashMap();
		map.put("key1", 100);
        map.put("key2",100);
        map.put("key3", "kdfajlsdfj");
        map.put(1, "wahahah");
        Set set=   map.keySet();
        Iterator  it= set.iterator();
        while(it.hasNext()){
        	System.out.println(map.get(it.next()));
        }
    Iterator  it=hashset.iterator();//迭代器  
    while(it.hasNext()){
 	   System.out.println(it.next());
    }
    */
}